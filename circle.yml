machine:
        services:
            -docker

        dependencies:

          override:
               -dockerinfo
               -docker build -t reddynithin84/pluralsight-dockerCI.




          deployment:

               hub:

                branch: master

                commands:

                  -dockerlogin -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
                  -docker push reddynithin84/pluralsight-dockerCI



                notify:


                        test:

                  override:
                          -mocha
